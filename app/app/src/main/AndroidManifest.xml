<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.poi.camppus">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo150"
        android:supportsRtl="true"
        android:theme="@style/Theme.Camppus">
        <activity android:name=".SendmessagesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainNavigationActivity"
            android:label="@string/title_activity_main_navigation">


        </activity>
        <activity android:name=".SignInActivity" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/SplashTheme">

        </activity>
    </application>

</manifest>